---
title: "Data Import"
output: html_document
---

```{r}
library(tidyverse)
```

Lte's import a data set. 
```{r}
litters_df = 
  read_csv("data/FAS_litters.csv")
```

Look at your data. 
```{r}
names(litters_df)
```

Update names in litters_df
```{r}
litters_df = 
  janitor::clean_names(litters_df)
```

Look at data again (lol)
```{r}
litters_df
```

Skimming data can be nice
```{r}
skimr::skim(litters_df)
```

 